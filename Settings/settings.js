const darkToggle = document.getElementById('toggleDark')

// Go back Button

/*
const backBtn = document.getElementById('goBack')
backBtn.addEventListener('click',e => { 
    window.location.href('../index.html')
})


// Boolien to track if dark mode is on or not
let darkOn = false


darkToggle.addEventListener('click',e => { 
    if (darkOn) { 
        console.log('Turning Dark Mode off')
        darkOn = false;

        // Set the App into dark mode
        document.getElementById('darkModeOn').style.display = 'none'
       
       // Set the text into Dark mode
        let allText = document.querySelectorAll('.text');

        
        allText.forEach(element => {
          element.style.color = 'black';
        });
        

    } else if (darkOn == false) { 
        console.log('Turning Dark mode on')
        darkOn = true;

        // Set the Page into Dark Mode
        document.getElementById('darkModeOn').style.display = 'block'

        // Set the Text into light mode
        let allText = document.querySelectorAll('.text');

// Loop through all elements with the class "text" and change their text color
allText.forEach(element => {
  element.style.color = 'white';
});






    }
})

*/