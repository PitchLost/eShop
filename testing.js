// I will have multiple elements in a parent div with the classes .price and .itemName 

// I somehow need to convert only the elements in the cart name to JSON using JSON.stringify 

// May be farely easy dunno 

// Testing: 

const cartParentDiv = document.getElementById('cartItems') 

// Define a Array to store the price in: 

let cartedItemPrice = []; 

function funServerStuff() { 

    
cartedItemPrice = cartParentDiv.childNodes.querySelector.array.forEach(element => {
    ('.price') 
});

console.log(cartedItemPrice)
}


// This could work however it will most likely put all the prices together instead of seperate like I need them to be